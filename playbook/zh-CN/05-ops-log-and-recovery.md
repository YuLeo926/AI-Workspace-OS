# Ops Log 和恢复

ops log 记录需要审计的动作，不是记录每一次编辑。

适合写入 `_ops_log/agent_action_log.jsonl` 的情况：

- 修改重要文件；
- 更新规则；
- 修改 ADR；
- 跨 owner 写入；
- 执行需要恢复上下文的动作。

一条好的记录应该包含：

- 时间；
- agent 或角色；
- 动作；
- 目标；
- 授权状态；
- 变更文件；
- 回滚提示；
- 简短说明；
- 可选 role check。

回滚提示要具体，方便另一个 agent 接手恢复。
